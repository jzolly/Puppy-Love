<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/indexHead.ejs') %> 
    <title>Possible Friends</title>
</head>
<body>
    <header>
        <%- include('../partials/nav.ejs') %>
    </header>
    <main>
        <h1>Pawsible Friends</h1>
        <ul>
            <% for(let i=0; i < possible.length; i++){ %>
                <li>
                    <a href="/possible/<%= possible[i]._id %>"><%= possible[i].name %></a> <br/>
                    <img class="doggo" src="<%= possible[i].img %>" height="200px"/>
                </li>
                <form action='/possible/<%= possible[i]._id %>?_method=DELETE' method="POST" style="display:inline; margin-left:16px;">
                    <input type="submit" value="X">
                </form>
                <form action="/possible/<%= possible[i]._id %>/heart" method="POST" style="display:inline; margin-left:16px;">
                    <button>Heart</button>
                </form>
           <% } %> 
        </ul>
    </main>
</body>
</html>