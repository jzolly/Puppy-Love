<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('../partials/indexHead.ejs') %> 
    <title>Possible Friends</title>
</head>
<body>
    <header>
        <%- include('../partials/nav.ejs') %>
    </header>
    <main>
        <h1>Pawsible Friends</h1>
        <ul>
            <% for(let i=0; i < possible.length; i++){ %>
                <li>
                    <a href="/possible/<%= possible[i]._id %>"><%= possible[i].name %></a> <br/>
                    <img  src="<%= possible[i].img %>" height="200px"/>
                </li>
           <% } %> 
        </ul>
    </main>
</body>
</html>